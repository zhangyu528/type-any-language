# Cloudflare Pages build config — read when this branch is connected
# via the GitHub integration.
#
# Cloudflare Pages reads wrangler.toml from the project root (which
# for us is `frontend/`, set via "Root directory" in the dashboard).
#
# GITHUB_PAGES is intentionally NOT set here — `build:pages` script
# in package.json already sets it, and the env-only `output: 'export'`
# mode in next.config.js doesn't need a basePath (Cloudflare URLs
# serve at root).
#
# Docs: https://developers.cloudflare.com/pages/functions/wrangler-configuration/

[build]
command = "npm run build:pages"
directory = "out"

[build.environment]
NEXT_PUBLIC_DEMO_MODE = "true"
NEXT_PUBLIC_API_URL = ""
